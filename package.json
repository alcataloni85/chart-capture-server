{
  "name": "chart-capture-server",
  "version": "1.0.0",
  "description": "TradingView chart capture server using Puppeteer + Supabase",
  "main": "src/index.js",
  "scripts": {
    "start": "npx puppeteer browsers install chrome && node src/index.js",
    "dev": "nodemon src/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "puppeteer-core": "^21.0.0",
    "uuid": "^9.0.0",
    "ws": "^8.20.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  },
  "engines": {
    "node": "18.x"
  }
}
